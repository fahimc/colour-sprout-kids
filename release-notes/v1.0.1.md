# ColourSprout Kids v1.0.1

Usability and art-source fix.

- Reworked the portrait colouring screen so the canvas takes the full screen with compact overlay controls instead of being squeezed into a tiny strip.
- Kept landscape focused on a large central canvas with side controls.
- Replaced procedural code-drawn starter pages with generated original line-art source sheets.
- Regenerated all 32 `line.png`, `mask.png`, `thumb.png`, metadata files, license manifest entries, and asset report from the generated source sheets.
- Enlarged colouring-page artwork so drawings fill the canvas properly.
